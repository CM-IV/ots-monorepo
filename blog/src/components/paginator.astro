---
const { page } = Astro.props;
---

<div>
  <p class="control">
    {page.url.prev && <a href={page.url.prev}>
      Newer Posts
    </a>}
  </p>
  <p class="control">
    {page.url.next && <a href={page.url.next}>
      Older Posts
    </a>}
  </p>
</div>